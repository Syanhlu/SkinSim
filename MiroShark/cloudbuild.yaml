steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - build
      - -f
      - Dockerfile.railway
      - -t
      - '$_IMAGE'
      - .
    timeout: 1200s

images:
  - '$_IMAGE'

timeout: 1800s

options:
  machineType: E2_HIGHCPU_8
