import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Each project forks this file. Add project-specific config (image domains,
  // rewrites to a companion Python/MiroShark service, etc.) here.
};

export default nextConfig;
